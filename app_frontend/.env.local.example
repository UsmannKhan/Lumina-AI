# Frontend environment configuration. Copy this file to `.env.local` and
# fill in real values.

# Backend API URL. For local development this is typically:
#   http://localhost:8000
# For production, set to the deployed backend URL (e.g. a Render / Fly /
# Railway / Vercel-API URL).
NEXT_PUBLIC_API_URL=http://localhost:8000
